## Packages
canvas-confetti | For the perfect stack burst effect
@types/canvas-confetti | Types for canvas-confetti

## Notes
- Using React state and Framer Motion for the UI layer
- Using `requestAnimationFrame` and `useRef` for the moving block to ensure 60fps performance without React render blocking
- Using standard `localStorage` alongside the `/api/scores` endpoint for persistence
